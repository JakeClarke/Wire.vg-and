<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="wirevg.apps.viewer">
      
    <application android:icon="@drawable/icon" android:label="@string/app_name" android:debuggable="false">
        <activity android:name="MainTabs"
                  android:label="@string/app_name" android:configChanges="orientation" android:theme="@android:style/Theme.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:label="@string/app_name" android:name=".trendViewActivity" android:configChanges="orientation" android:theme="@style/windu">
            <intent-filter>
                
                
            </intent-filter>
        </activity>

    <activity android:name=".ListPosts" android:configChanges="orientation" android:theme="@style/windu"></activity>
<activity android:name="PostDetailsActivity" android:theme="@style/windu"></activity>
</application>



<uses-permission android:name="android.permission.INTERNET"></uses-permission>
<uses-sdk android:targetSdkVersion="7" android:minSdkVersion="4"></uses-sdk>
</manifest> 